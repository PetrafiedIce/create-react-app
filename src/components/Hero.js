import React from 'react';
import './Hero.css';

const Hero = () => (
  <section className="hero">
    <h2>Welcome to BlockRealm</h2>
    <p>Your next adventure awaits.</p>
    <a href="#join" className="cta">Join Now</a>
  </section>
);

export default Hero;
