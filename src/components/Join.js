import React from 'react';
import './Join.css';

const Join = () => (
  <section id="join" className="join">
    <h2>Join the Server</h2>
    <p>IP: play.blockrealm.example</p>
  </section>
);

export default Join;
